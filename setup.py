from setuptools import setup, find_packages

# Folder name here, within this project
NAME = 'bisparktest'

REQUIRES = [
]

DEPENDENCY_LINKS = []

setup(
    name=NAME,
    version="0.1.0",
    packages=find_packages(),
    install_requires=REQUIRES,
    dependency_links=DEPENDENCY_LINKS,
    description='',
    author='alessandro@hellofresh.com'
)